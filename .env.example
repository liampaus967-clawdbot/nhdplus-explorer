# ===========================================
# River Router Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in values
# NEVER commit .env.local to git!

# Mapbox Public Token (required)
# Get from: https://mapbox.com/account/access-tokens
# This is exposed to the client (NEXT_PUBLIC_ prefix)
# Secure it by restricting to your domain in Mapbox dashboard
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_token_here

# PostgreSQL Database (required)
# Must have PostGIS extension enabled
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@host:5432/database

# CORS Origin (optional, recommended for production)
# Lock API access to your domain only
# Default: * (allows all origins)
ALLOWED_ORIGIN=https://yourdomain.com

# Password Protection (required)
# Used for basic auth - username can be anything, password must match
AUTH_PASSWORD=your_password_here
