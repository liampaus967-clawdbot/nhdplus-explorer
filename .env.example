# Mapbox token (required for map display)
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_mapbox_token_here

# API Security (optional - leave empty for open access during development)
API_KEY=

# Rate limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60
